"""Minimal inert shell for the Tree Counter QGIS plugin."""

# SPDX-License-Identifier: AGPL-3.0-only


class TreeCounterPlugin:
    """QGIS plugin shell reserved for the future counting workflow."""

    def __init__(self, iface):
        self.iface = iface

    def initGui(self) -> None:
        """Keep the pre-feature shell deliberately inert."""

    def unload(self) -> None:
        """Keep the pre-feature shell deliberately inert."""
